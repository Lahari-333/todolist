# todolist
The Multi-Tenant Task Management System allows multiple organizations to manage tasks securely within a single platform. It ensures data isolation using org_id and implements role-based access control (Admin/Member). Users can perform CRUD operations and track changes via audit logs, demonstrating key SaaS concepts.
